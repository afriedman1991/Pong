using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace gamingaf
{
    public class Goal : MonoBehaviour
    {
        private void Update()
        {
            //if (ball.transform.position.x < GameManager.bottomLeft.x + radius && direction.x < 0)
            //{
            //    Debug.Log("Right player wins!");
            //}
        }
    }
}
